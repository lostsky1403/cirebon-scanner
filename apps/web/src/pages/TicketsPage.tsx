import { useQuery } from "@tanstack/react-query";
import { DownloadSimple, MagnifyingGlass } from "@phosphor-icons/react";
import { useState } from "react";
import { api } from "../lib/api";
type TicketRow = { id:string; orderId:string; ticketCode:string; participantName:string; whatsapp:string; email:string; paymentStatus:string; present:boolean; checkedInAt?:string };
export function TicketsPage() { const [q,setQ]=useState(""); const tickets=useQuery({queryKey:["tickets",q],queryFn:()=>api<TicketRow[]>(`/admin/tickets?q=${encodeURIComponent(q)}`)}); return <><header className="page-header"><div><p className="kicker">DATABASE PESERTA</p><h1>Peserta & Tiket</h1></div><a className="button dark" href="/api/admin/exports/check-ins.csv"><DownloadSimple/>Ekspor CSV</a></header><div className="toolbar"><MagnifyingGlass/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari nama, kode tiket, order ID, WhatsApp, email…"/></div><div className="table-wrap"><table><thead><tr><th>Peserta</th><th>Kode</th><th>Kontak</th><th>Pembayaran</th><th>Kehadiran</th></tr></thead><tbody>{tickets.data?.map(t=><tr key={t.id}><td><strong>{t.participantName}</strong><small>{t.orderId}</small></td><td className="mono">{t.ticketCode}</td><td><span>{t.whatsapp}</span><small>{t.email}</small></td><td><span className={`badge ${t.paymentStatus}`}>{t.paymentStatus}</span></td><td><span className={`badge ${t.present?"paid":"muted"}`}>{t.present?"Hadir":"Belum"}</span>{t.checkedInAt&&<small>{new Date(t.checkedInAt).toLocaleString("id-ID")}</small>}</td></tr>)}</tbody></table></div></>; }
